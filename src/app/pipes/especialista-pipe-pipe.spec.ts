import { EspecialistaPipePipe } from './especialista-pipe-pipe';

describe('EspecialistaPipePipe', () => {
  it('create an instance', () => {
    const pipe = new EspecialistaPipePipe();
    expect(pipe).toBeTruthy();
  });
});
